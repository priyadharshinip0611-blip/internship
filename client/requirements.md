## Packages
framer-motion | Page transitions and detailed animations
lucide-react | Iconography for the UI
clsx | Conditional class merging
tailwind-merge | Class merging utility
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for forms
zod | Schema validation

## Notes
- Theme colors: Earthy greens (primary), warm browns (secondary), clean whites/creams (backgrounds).
- Fonts: 'Outfit' for headings, 'Inter' for body text.
- Auth flow uses specific endpoint structure /api/register and /api/login.
- Admin dashboard requires specific approval workflow.
